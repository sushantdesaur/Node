const num1 = 10;
const num2 = 30;

function addvalues() {
    console.log(`The sum is equal to : ${num1+num2}`);
};

// If the function is invoked as shown in the next line, it will directly execute in this case app.js
addvalues(); // Direct execution for both local and module exports.