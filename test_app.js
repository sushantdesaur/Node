// Local modules are imported using require('./module_name')
// To move up a directory use ../
// Built in modules are imported using require('module_name')

const names = require('./2-names');
const sayHi = require('./5-utils'); 
const data = require('./6-alternative-flavor');

require('./7-mind-grenade');



// console.log(data);

// sayHi('John');

// sayHi(names.john);
